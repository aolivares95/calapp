export class Calevent {
  constructor(id, title, uuid, start, end, allday) {
    this.id = id;
    this.title = title;
    this.uuid = uuid;
    this.start = start;
    this.end = end;
    this.allday = allday;
  }
}
